{
    "id": "docker-from-docker",
    "version": "1.0.0",
    "name": "Docker from Docker",
    "description": "Access your host's Docker install from inside a dev container. Installs Docker extension in the container along with needed CLIs.",
    "documentationURL": "https://github.com/devcontainers/templates/tree/main/src/docker-from-docker",
    "publisher": "Dev Container Spec Maintainers",
    "licenseURL": "https://github.com/devcontainers/templates/LICENSE",
    "type": "image",
    "options": {
        "installZsh": {
            "type": "boolean",
            "description": "Install ZSH!?",
            "default": "true",
            "replaceIn": [
                ".devcontainer.json"
            ]
        },
        "upgradePackages": {
            "type": "boolean",
            "description": "Upgrade OS packages?",
            "default": "false",
            "replaceIn": [
                ".devcontainer.json"
            ]
        },
        "dockerVersion": {
            "type": "string",
            "proposals": [
                "latest",
                "none",
                "20.10"
            ],
            "default": "latest",
            "description": "Select or enter a Docker/Moby CLI version. (Availability can vary by OS version.)",
            "replaceIn": [
                ".devcontainer.json"
            ]
        },
        "moby": {
            "type": "boolean",
            "default": "true",
            "description": "Install OSS Moby build instead of Docker CE",
            "replaceIn": [
                ".devcontainer.json"
            ]
        },
        "enableNonRootDocker": {
            "type": "boolean",
            "description": "Enable non-root Docker access in container?",
            "default": "true",
            "replaceIn": [
                ".devcontainer.json"
            ]
        }
    },
    "platforms": [
        "Any"
    ]
}
