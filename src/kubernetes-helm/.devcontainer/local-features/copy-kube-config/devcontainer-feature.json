{
	"id": "copy-kube-config",
	"name": "Copies kube config files",
	"options": {
		"username": {
			"type": "string",
			"default": "vscode",
			"description": "Enter name of non-root user to configure or none to skip"
		}
	}
}
